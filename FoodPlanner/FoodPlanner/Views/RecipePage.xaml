<Page
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ViewModels="clr-namespace:FoodPlanner.ViewModels" x:Class="FoodPlanner.Views.RecipePage" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="RecipePage">

   <!-- <Page.DataContext>
        <ViewModels:RecipeViewModel/>
    </Page.DataContext>-->

    <Grid DataContext="{Binding Recipe}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="1" Grid.RowSpan="2">
            <Label Content="{Binding Title}" HorizontalAlignment="Left" Margin="0" VerticalAlignment="Top"/>
            <TextBlock Text="{Binding Preparation}" Grid.Column="1" Margin="0" TextWrapping="Wrap"/>
        </StackPanel>

        <Image HorizontalAlignment="Left" Height="100" Margin="5" VerticalAlignment="Top" Width="100"/>
        <ListBox ItemsSource="{Binding RecipeIngredients}" Margin="5" Grid.Row="1"/>
    </Grid>
</Page>
