<Page
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ViewModels="clr-namespace:FoodPlanner.ViewModels" x:Class="FoodPlanner.Views.RecipePage" 
      mc:Ignorable="d" 
      d:DesignHeight="720" d:DesignWidth="405"
	Title="RecipePage">

    <!-- <Page.DataContext>
        <ViewModels:RecipeViewModel/>
    </Page.DataContext>-->

    <Grid DataContext="{Binding Recipe}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Image Height="100" Width="100" Source="{Binding Image}"/>

        <Label Grid.Column="1" Grid.Row="0" Style="{StaticResource LabelTitle}">
            <Label.Content>
                <AccessText TextWrapping="Wrap" Text="{Binding Title}"/>
            </Label.Content>
        </Label>

        <ScrollViewer Grid.Column="1" Grid.Row="1" Padding="5,0,0,0">
            <TextBlock Text="{Binding RecipesPreparation.Preparation}" TextWrapping="Wrap"/>
        </ScrollViewer>

        <ListBox x:Name="IngredientListbox" ItemsSource="{Binding RecipeIngredients}" ScrollViewer.VerticalScrollBarVisibility="Disabled" Grid.Row="1" Width="100" Background="{StaticResource BackgroundColour}" FocusManager.FocusedElement="{Binding RelativeSource={RelativeSource Self}}">
            <ListBox.ItemTemplate>    
            <DataTemplate>
                    <StackPanel>
                        <Border x:Name="IngredientBorder" BorderBrush="{StaticResource Button.Static.Border}" BorderThickness="0 0 0 1" Width="{Binding ElementName=IngredientText}">
                            <TextBlock Width="100" Name="IngredientText"  Text="{Binding}" TextWrapping="Wrap"/>
                        </Border>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>
